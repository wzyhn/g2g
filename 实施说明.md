# 挂2过(G2G)· 项目实施说明

> **目的**:本文档是项目从"原型"到"上线"的完整施工说明,交给下一位 AI(或人)执行剩余工作。
>
> **前序工作**:已由 Claude 4.7 Opus 完成 HTML 原型 `python-review.html` 与 `README.md`。

---

## 0. 速览(TL;DR)

| 项 | 内容 |
|---|---|
| 项目名 | **挂2过** · 简称 **G2G** · 副标题"从挂到过" |
| 场景 | 河北工程大学计算机系《Python 程序设计》期末复习 |
| 项目启动 | 2026-05-17 21:00 |
| 考试时间 | 2026-05-19 09:00 |
| 用户 | 本班同学(约 90 人,以手机+微信浏览器为主) |
| 题量 | 选择题 373 道(去重) + 编程题 41 道 |
| 已交付 | `python-review.html`(单文件原型)、`README.md` |
| 待完成 | GitHub 仓库、真实数据导入、AI 答案批量生成、部署上线 |
| 约束 | 零后端、零依赖、移动端优先、单文件 ≤ 100KB |

---

## 一、项目背景

学校 e2e 教学平台抓下来的 373 道选择题 + 41 道编程题,题目本身在 16 个 `.md` 文件里,但**没有官方答案**。本项目要做的是:把这堆 .md 灌入一个移动端友好的刷题站,用 AI 推出答案与讲解,让全班同学考前能集中刷错题。

### 用户痛点

1. 没有答案,自己想 1 题要 5 分钟
2. 手机刷题工具体验差,代码题尤其难看
3. 老师跳过了选择题 ch04(82 题)和编程题 ch05(3 题),没人讲过

### 项目约束

- **6 天内必须上线**(实际只剩 36 小时)
- **零后端**:静态托管,localStorage 存进度
- **移动端优先**:微信内置浏览器、Chrome、Safari、360 等都得跑
- **零盗版风险**:仅供同班学习,不能商业化

---

## 二、当前交付物清单

| 文件 | 状态 | 说明 |
|---|---|---|
| `python-review.html` | ✅ 完成 | 单文件 HTML 原型,包含全部 UI 和交互逻辑,内嵌 6 道样本题 |
| `README.md` | ✅ 完成 | GitHub 仓库门面 |
| `实施说明.md` | ✅ 完成 | 本文件 |
| `data/questions.json` | ⬜ 待生成 | 用户原始 .md → 由 `md_to_json.py` 生成 |
| `data/questions.json`(含 AI 答案) | ⬜ 待生成 | 用第七节的提示词批量喂给 AI |
| `scripts/md_to_json.py` | ⬜ 待生成 | 见第六节,本文档已附完整代码 |
| 真实部署 URL | ⬜ 待上线 | 用户自有服务器+域名 |
| GitHub 公开仓库 | ⬜ 待创建 | 仓库名建议 `g2g`,见第九节 |

---

## 三、目标文件结构

```
g2g/                              # GitHub 仓库根目录
├── index.html                    # 由 python-review.html 改名
├── README.md
├── 实施说明.md                    # 本文件
├── data/
│   └── questions.json            # 由脚本生成 + AI 填充
├── scripts/
│   └── md_to_json.py             # MD 转 JSON 脚本
├── review/                       # 用户原始题库(可选 commit)
│   ├── 选择题/
│   │   ├── ch01.md
│   │   └── ...
│   └── 编程题/
│       ├── ch01.md
│       └── ...
└── docs/
    ├── home.png                  # README 截图(后补)
    ├── quiz.png
    └── mistakes.png
```

---

## 四、待办清单(按优先级)

### P0 — 必做(36 小时内)

- [ ] **GitHub 公开仓库**创建,推 `index.html` + `README.md`
- [ ] 修改 `index.html` 顶部 `CONFIG.github` 为实际仓库 URL
- [ ] 实现 `scripts/md_to_json.py` 并运行,产出 `data/questions.json` 框架
- [ ] 用第七节提示词,批量喂给 AI 填充 373 道选择题的 `answer` / `confidence` / `topic` / `explanation`
- [ ] 用同样方式填充 41 道编程题的 `bug` / `fixed` / `topic`
- [ ] **修改 `index.html` 数据加载方式**:从内嵌 `DATA` 常量改为 `fetch('data/questions.json')`
- [ ] **人工核对**老师未讲的 ch04 选择题(82 道)和编程 ch05 题(3 道) —— 这两块如果考到,杀伤力最大
- [ ] 部署到用户服务器,绑定域名,配置 HTTPS
- [ ] 在线访问 URL 写入 README 顶部和 HTML 底部 footer

### P1 — 重要(部署后)

- [ ] 抽样核对 ch01-ch03 / ch05-ch10 的高置信答案(每章随机 5 道)
- [ ] 修复发现的错误答案,提交到 GitHub
- [ ] 截图首页 / 做题页 / 错题本,放到 `docs/`,更新 README
- [ ] 在班群发布链接 + 简短使用说明

### P2 — 锦上添花(考完前如有余裕)

- [ ] PWA 配置(`manifest.json` + 几行 meta) → 让用户"添加到主屏幕"后像原生 App
- [ ] 错题截图分享(`html2canvas`) → 把错题本渲染成 PNG 发到微信群

### 不要做的事

- ❌ 不要换框架(React / Vue / Svelte 都不要)
- ❌ 不要换字体(系统字体已经够,加载 web font 在国内网络会变慢)
- ❌ 不要重设计 UI(已有视觉是产品的一部分)
- ❌ 不要移除 Claude 4.7 Opus 署名(透明度)
- ❌ 不要加注册/登录/账户系统(零后端是硬约束)
- ❌ 不要把答案的"置信度"字段藏起来(用户需要知道哪些答案不靠谱)

---

## 五、数据格式规范

### 5.1 `data/questions.json`(单文件,前端一次 fetch)

```json
{
  "meta": {
    "choiceChapters": [
      { "id": "ch01", "name": "Python 概述", "count": 28, "skipped": false },
      { "id": "ch04", "name": "程序的控制结构", "count": 82, "skipped": true }
    ],
    "codeChapters": [
      { "id": "ch01", "name": "基础语法", "count": 8, "skipped": false }
    ]
  },
  "choice": [
    {
      "id": "ch01-q003",
      "ch": "ch01",
      "stem": "以下哪个选项不是 Python 语言的保留字?",
      "options": ["True", "false", "None", "and"],
      "answer": 1,
      "confidence": "high",
      "topic": "保留字",
      "explanation": "<p>Python 中的布尔值保留字是首字母大写的 <code>True</code>...</p>"
    }
  ],
  "code": [
    {
      "id": "0401",
      "ch": "ch04",
      "title": "温度转换",
      "score": 66,
      "problem": "输入一个摄氏温度 C,计算华氏温度 F = C*9/5+32...",
      "submitted": "a=int(input(\"输入摄氏温度C:\"))\nf=a*9/5+32\n...",
      "bug": "测试用例输入 <code>32.3</code>,<code>int()</code> 抛 ValueError...",
      "fixed": "a=float(input(\"输入摄氏温度C:\"))\nf=a*9/5+32\n...",
      "topic": ["类型转换", "input 返回字符串"]
    }
  ]
}
```

### 5.2 字段说明

**`choice[].answer`**: `0|1|2|3` 分别代表 A/B/C/D。

**`choice[].confidence`**: 必须是字符串之一:
- `"high"` —— 多模型一致 + 题面清晰 + 不在未讲章节
- `"medium"` —— 模型有把握但题面略有歧义、或在老师未讲章节
- `"low"` —— 模型不确定、题面有错别字、选项写法不规范

**`choice[].topic`**: 简短词组,如"保留字"、"列表切片"、"默认参数"等。前端会显示在讲解 summary 旁边。

**`choice[].explanation`**: 允许 HTML 片段(`<p>`、`<code>`、`<b>`、`<br>`),前端直接 `innerHTML` 渲染。**不要超过 200 字**,移动端阅读体验最优。

**`code[].topic`**: 数组,如 `["类型转换", "input 返回字符串"]`。

**`code[].bug` / `code[].fixed`**: 同 explanation,允许 HTML。`fixed` 字段里的代码会被前端用 `<pre>` 包,所以**不要**自己加 `<code>` 或 `<pre>` 标签。

### 5.3 ID 规则

- 选择题 ID:`{ch}-q{nnn}`,如 `ch01-q003`,数字三位补零,**保持稳定** —— 错题本基于 ID 持久化,改了 ID 用户错题会丢
- 编程题 ID:四位数字字符串,如 `"0401"`,沿用学校 e2e 平台原编号

---

## 六、Markdown → JSON 转换

### 6.1 用户的 .md 文件格式

**选择题**(`review/选择题/ch01.md`):
```markdown
## 3. 以下哪个选项不是 Python 语言的保留字?( )
- (A) True
- (B) false
- (C) None
- (D) and

## 4. 下列哪个是 Python 中合法的变量名?( )
- (A) 2score
- (B) _score
- (C) score-1
- (D) for
```

**编程题**(`review/编程题/ch04.md`):
````markdown
## 0401 温度转换
**状态**: ⚠️ 得分 66  |  **截止**: 2026/6/30  |  **Eid**: 5662

### 题目
输入一个摄氏温度 C,计算华氏温度 F = C*9/5+32,输出保留 2 位小数。

### 我提交的代码
```python
a=int(input("输入摄氏温度C:"))
f=a*9/5+32
print("华氏温度F:%.2f"%f)
```

## 0402 及格判定
...
````

### 6.2 `scripts/md_to_json.py`(完整代码,直接保存可用)

```python
#!/usr/bin/env python3
"""
md_to_json.py — 把 review/ 下的 markdown 题目转成 data/questions.json

用法:
    python scripts/md_to_json.py

输入:
    review/选择题/ch01.md ... ch10.md
    review/编程题/ch01.md ... ch06.md

输出:
    data/questions.json
"""
import re
import json
from pathlib import Path

# === 配置 ===
ROOT = Path(__file__).resolve().parent.parent
REVIEW_DIR = ROOT / "review"
OUTPUT_DIR = ROOT / "data"

# 老师跳过的章节 —— 前端会显示"未讲"标签
SKIPPED_CHOICE = {"ch04"}
SKIPPED_CODE   = {"ch05"}

# 章节中文名 —— 嵩天《Python 程序设计》(高教社)常规目录
# 与你的实际课本对照后修改
CHAPTER_NAMES = {
    "ch01": "Python 概述",
    "ch02": "Python 基本图形绘制",
    "ch03": "基本数据类型",
    "ch04": "程序的控制结构",
    "ch05": "函数与代码复用",
    "ch06": "组合数据类型",
    "ch07": "文件与数据格式化",
    "ch08": "程序设计方法学",
    "ch09": "Python 计算生态",
    "ch10": "Python 标准库",
}

# === 选择题解析 ===
CHOICE_BLOCK = re.compile(
    r"##\s+(?P<num>\d+)\.\s+(?P<stem>.+?)\n"
    r"(?P<opts>(?:\s*-\s*\([A-D]\).+\n?)+)",
    re.DOTALL,
)
OPTION_RE = re.compile(r"-\s*\(([A-D])\)\s*(.+)")

def parse_choice_file(text: str, ch: str) -> list[dict]:
    out = []
    for m in CHOICE_BLOCK.finditer(text):
        num = m.group("num")
        stem = m.group("stem").strip()
        # 去掉题面末尾的 "( )" 占位
        stem = re.sub(r"\(\s*\)\s*$", "", stem).strip()
        options = []
        for line in m.group("opts").strip().splitlines():
            om = OPTION_RE.search(line.strip())
            if om:
                options.append(om.group(2).strip())
        if len(options) != 4:
            print(f"  ⚠ {ch}#{num} 选项数量异常({len(options)}),跳过")
            continue
        out.append({
            "id": f"{ch}-q{num.zfill(3)}",
            "ch": ch,
            "stem": stem,
            "options": options,
            "answer": None,         # ← AI 待填
            "confidence": "low",    # ← AI 待填
            "topic": "",            # ← AI 待填
            "explanation": "",      # ← AI 待填
        })
    return out

# === 编程题解析 ===
CODE_BLOCK = re.compile(
    r"##\s+(?P<id>\d{4})\s+(?P<title>.+?)\n"
    r"(?P<body>.*?)(?=\n##\s+\d{4}|\Z)",
    re.DOTALL,
)
SCORE_RE   = re.compile(r"得分\s*(\d+)")
PROBLEM_RE = re.compile(r"###\s*题目\s*\n(.+?)(?=###|\Z)", re.DOTALL)
CODE_RE    = re.compile(r"###\s*我提交的代码\s*\n```(?:python)?\s*\n(.+?)```", re.DOTALL)

def parse_code_file(text: str, ch: str) -> list[dict]:
    out = []
    for m in CODE_BLOCK.finditer(text):
        body = m.group("body")
        score = int(SCORE_RE.search(body).group(1)) if SCORE_RE.search(body) else 100
        problem = PROBLEM_RE.search(body)
        submitted = CODE_RE.search(body)
        out.append({
            "id": m.group("id"),
            "ch": ch,
            "title": m.group("title").strip(),
            "score": score,
            "problem": problem.group(1).strip() if problem else "",
            "submitted": submitted.group(1).rstrip() if submitted else "",
            "bug": "",      # ← AI 待填
            "fixed": "",    # ← AI 待填
            "topic": [],    # ← AI 待填
        })
    return out

# === 主流程 ===
def main():
    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
    
    print("处理选择题...")
    choice_chapters, all_choice = [], []
    for f in sorted((REVIEW_DIR / "选择题").glob("ch*.md")):
        ch = f.stem
        qs = parse_choice_file(f.read_text(encoding="utf-8"), ch)
        choice_chapters.append({
            "id": ch,
            "name": CHAPTER_NAMES.get(ch, ch),
            "count": len(qs),
            "skipped": ch in SKIPPED_CHOICE,
        })
        all_choice.extend(qs)
        print(f"  · {ch}: {len(qs)} 题")
    
    print("\n处理编程题...")
    code_chapters, all_code = [], []
    for f in sorted((REVIEW_DIR / "编程题").glob("ch*.md")):
        ch = f.stem
        qs = parse_code_file(f.read_text(encoding="utf-8"), ch)
        code_chapters.append({
            "id": ch,
            "name": CHAPTER_NAMES.get(ch, ch),
            "count": len(qs),
            "skipped": ch in SKIPPED_CODE,
        })
        all_code.extend(qs)
        print(f"  · {ch}: {len(qs)} 题")
    
    out = {
        "meta": {
            "choiceChapters": choice_chapters,
            "codeChapters": code_chapters,
        },
        "choice": all_choice,
        "code": all_code,
    }
    (OUTPUT_DIR / "questions.json").write_text(
        json.dumps(out, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    
    print(f"\n✓ 共 {len(all_choice)} 选择题 + {len(all_code)} 编程题")
    print(f"  已写入 {OUTPUT_DIR / 'questions.json'}")
    print(f"\n下一步:用「实施说明.md 第七节」的提示词喂给 AI,填充 answer / explanation 字段")

if __name__ == "__main__":
    main()
```

### 6.3 运行方式

```bash
cd g2g/
python scripts/md_to_json.py
```

预期输出:
```
处理选择题...
  · ch01: 28 题
  · ch02: 42 题
  ...
✓ 共 373 选择题 + 41 编程题
  已写入 data/questions.json
```

如果某些题解析失败(选项数异常等),脚本会打 warning,人工核对一下原始 .md 即可。

---

## 七、AI 批量生成答案

### 7.1 选择题答案生成(主提示词)

**使用方法**:把生成好的 `data/questions.json` 里的 `choice` 字段切片(每批 20-25 道),把以下提示词 + 切片喂给 Claude / 豆包 / Kimi。建议**用 Claude 4.7 Opus 或同级模型主跑**,然后用另一个模型(Kimi 或 GPT-4)随机抽 30 道复跑,交叉比对。

```
你是一位资深的 Python 教师,正在帮河北工程大学计算机系的同学准备《Python 程序设计》期末考试。

我会给你一批选择题(JSON 数组),请为每道题输出:
- answer:正确选项的下标,0=A, 1=B, 2=C, 3=D
- confidence:置信度,只能是 "high" / "medium" / "low"
- topic:考点,2-6 个汉字的简短词组
- explanation:讲解,HTML 片段,80-150 字,允许 <p> <code> <b> <br>

置信度判定规则:
- high:你 100% 确定答案,题面清晰无歧义
- medium:你有把握但题面有轻微歧义,或考查的是较为高阶的语法(如装饰器、生成器、metaclass)
- low:你不确定,或题面有错别字、选项写法不规范

讲解要求:
- 先一句说出为什么这个答案对
- 再用 1-2 句说其他错误选项错在哪
- 末尾给一个易错点或对比知识点
- 全程中文,语气像在面对面讲题

输出格式:**严格的 JSON 数组**,可以被 JSON.parse,不要任何 markdown 包裹、不要任何额外说明文字。结构:

```json
[
  {
    "id": "ch01-q003",
    "answer": 1,
    "confidence": "high",
    "topic": "保留字",
    "explanation": "<p>Python 的布尔值保留字是首字母大写的 <code>True</code> 和 <code>False</code>。B 写的是小写 <code>false</code>,只是一个普通标识符。</p><p><b>易错点</b>:Python 大小写敏感,跟 C/Java 不同。</p>"
  }
]
```

题目(JSON):
[此处粘贴 20-25 道题的 JSON 切片]

请处理。
```

### 7.2 编程题分析提示词

```
你是一位资深的 Python 教师。我会给你一批学生提交的代码题,每道题包含:题面 + 学生提交的代码 + 得分。

请为每道题输出:
- bug:学生代码哪里错了,HTML 片段,简短(50-100 字),允许 <code> 标签
- fixed:修正后的完整代码字符串(纯文本,不要加 markdown 代码块包装)
- topic:考点数组,每个元素 2-6 字,如 ["类型转换", "input 返回字符串"]

如果学生得分 = 100,bug 写"满分,无需修改",fixed 同 submitted。
如果学生得分 < 60,通常是逻辑错误(把及格线写错、判断条件反了等),仔细对比题面找问题。
如果学生得分 60-99,通常是边界 case 没处理(如题面要 float 输入而学生用 int)。

输出格式:严格 JSON 数组,可被 JSON.parse,不要 markdown 包裹。

```json
[
  {
    "id": "0401",
    "bug": "题面输入 <code>32.3</code> 是浮点数,但学生用 <code>int()</code> 转换,会抛 ValueError。",
    "fixed": "a=float(input(\"输入摄氏温度C:\"))\nf=a*9/5+32\nprint(\"华氏温度F:%.2f\"%f)",
    "topic": ["类型转换", "input 返回字符串"]
  }
]
```

题目(JSON):
[此处粘贴编程题的 JSON 切片]

请处理。
```

### 7.3 质量控制流程

**强制要求**:
1. **ch04 选择题(82 道,老师未讲)100% 人工核对** —— 这是全项目最易出问题的地方
2. **编程题 ch05(3 道,老师未讲)100% 人工核对**

**抽检要求**:
3. 其他章节的选择题,每章随机抽 5 道人工核对,发现错误率 > 20% 则整章重跑
4. 编程题人工至少看一遍 `bug` 描述是否合理

**置信度兜底**:
5. AI 标 `medium` 或 `low` 的题,在前端会用黄色 / 红色点显示,提醒用户"AI 答案,需注意"——这个机制已经在 HTML 里实现,**不要改**

**人工修正的去向**:
6. 修正后的答案,直接编辑 `data/questions.json` 提交 PR,不需要重跑脚本

---

## 八、接入真实数据(改 HTML)

### 8.1 修改 `index.html` 的数据加载逻辑

**当前(原型)**:
```javascript
const DATA = {
  meta: { ... },
  choice: [ ... 6 道样本 ],
  code: [ ... 2 道样本 ]
};
```

**改成**:
```javascript
let DATA = null;

async function loadData() {
  try {
    const r = await fetch('data/questions.json');
    if (!r.ok) throw new Error(r.statusText);
    DATA = await r.json();
    render();
  } catch (e) {
    document.getElementById('app').innerHTML = `
      <div style="padding: 60px 24px; text-align: center; color: var(--text-2);">
        <div style="font-family: var(--font-serif); font-size: 28px; margin-bottom: 12px;">题库加载失败</div>
        <div style="font-size: 14px;">${e.message}</div>
        <button class="cta" onclick="location.reload()" style="margin-top: 24px;">重试</button>
      </div>
    `;
  }
}
```

**修改 init 部分**:把原来的 `setTheme(state.theme); render();` 改成:
```javascript
setTheme(state.theme);
// 显示一个简单的 loading,避免白屏
document.getElementById('app').innerHTML = `
  <div style="padding: 100px 24px; text-align: center; color: var(--text-3); font-family: var(--font-serif); letter-spacing: 0.15em;">
    加载中…
  </div>
`;
loadData();
```

**注意**:`render()` 内已有 `if (!state.agreed && state.route !== "welcome") state.route = "welcome";` 这行,不用动。loadData 完成后 render 会自动走 welcome 或 home 分支。

### 8.2 保留 fallback

如果用户希望在 fetch 失败时仍能用样本题(单 HTML 文件可独立运行),可以让 DATA 默认是当前的样本数据,fetch 成功后覆盖。但这会让 HTML 文件膨胀,**不推荐**。

---

## 九、GitHub 仓库创建

### 9.1 步骤

1. 登录 GitHub,**New repository**
2. 仓库名:`g2g`(或你喜欢的)
3. 设为 **Public**,不要勾"Add README"(我们自己有)
4. 创建后,本地:
```bash
cd g2g/
git init
git add .
git commit -m "init: 挂2过 v0.1 — 由 Claude 4.7 Opus 生成"
git branch -M main
git remote add origin git@github.com:wzyhn/g2g.git
git push -u origin main
```

5. **修改 `index.html` 顶部 `CONFIG.github`** 为真实 URL 后,再 commit 一次:
```bash
git add index.html
git commit -m "chore: 更新 GitHub 仓库链接"
git push
```

### 9.2 推送内容清单

```
g2g/
├── index.html              # 必须
├── README.md               # 必须
├── 实施说明.md              # 必须(透明度)
├── data/questions.json     # 必须(部署后才有意义)
├── scripts/md_to_json.py   # 必须
├── docs/*.png              # 可选,部署后补
└── review/                 # 可选 —— commit 题库原始数据需慎重
                            # 学校题目版权,建议加 .gitignore 或只放空目录
```

**建议**:`review/` 加入 `.gitignore`,避免把学校题库公开传上 GitHub(版权风险)。

`.gitignore` 内容:
```
review/
*.DS_Store
__pycache__/
*.pyc
.vscode/
.idea/
```

---

## 十、部署清单

用户有自己的服务器和域名,假设服务器是 Linux + Nginx。

### 10.1 上传文件

```bash
# 本地打包
tar czf g2g.tar.gz index.html data/

# 上传
scp g2g.tar.gz user@your-server:/tmp/

# 服务器端
ssh user@your-server
sudo mkdir -p /var/www/g2g
sudo tar xzf /tmp/g2g.tar.gz -C /var/www/g2g/
```

### 10.2 Nginx 配置

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    root /var/www/g2g;
    index index.html;
    
    # 静态资源缓存
    location ~* \.(json|html)$ {
        expires 1h;
        add_header Cache-Control "public, must-revalidate";
    }
    
    # 单页应用,所有路由都回 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # gzip
    gzip on;
    gzip_types text/css application/javascript application/json text/html;
}

server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$host$request_uri;
}
```

```bash
sudo nginx -t && sudo nginx -s reload
```

### 10.3 备用方案:Cloudflare Pages

如果不想配 Nginx:
1. 把 `index.html` + `data/` 推到 GitHub
2. Cloudflare Dashboard → Pages → Connect to Git → 选 `g2g` 仓库
3. Build command 留空,Build output 填 `/`
4. 5 分钟内自动部署,送免费 HTTPS 与全球 CDN

---

## 十一、设计决策(请勿改动)

执行 AI 接手时,**这些是已经做过的权衡**,请不要"优化"它们:

1. **不接 OpenAI / Anthropic API**:学生没 API Key、成本不可控。改为复制提示词到豆包/Kimi
2. **零后端**:静态托管,localStorage 存进度。**不要加注册/登录/云同步**
3. **不做艾宾豪斯间隔重现**:6 天不够生效。错题池优先 + 重排即可
4. **不用框架**(React/Vue/Svelte 都不要):部署/构建/调试任何一环都可能炸 6 天计划
5. **不加载 web fonts**:国内 Google Fonts 不稳。系统字体已经够
6. **配色 / 字号 / 间距已调好**:不要"重设计"
7. **保留 Claude 4.7 Opus 署名**:出现在欢迎页、首页 footer、设置页关于。这是透明度,不是广告
8. **彩蛋是产品的一部分**:三态倒计时、顽疾标签、入场动画都不要去除
9. **欢迎页 = 温馨提示 + 免责声明 + AI 署名**:首次访问必看,这是合规要求
10. **置信度字段必须可见**:用户必须知道哪些答案是 AI 推断的

---

## 十二、推荐执行顺序

按"先有线上 URL,再灌数据"的原则:

```
Step 1 (10 min)  创建 GitHub 仓库,推 index.html + README + 实施说明.md
Step 2 (15 min)  改 CONFIG.github,部署到 Cloudflare Pages 或服务器,拿到 URL
Step 3 (30 min)  跑 md_to_json.py,生成 questions.json 的"骨架"
Step 4 (3-5 h)   用第七节提示词,批量喂 AI 生成 373 道选择题答案
Step 5 (1-2 h)   用第七节提示词,批量生成 41 道编程题分析
Step 6 (1.5 h)   100% 人工核对 ch04 选择题 + ch05 编程题
Step 7 (30 min)  改 HTML 数据加载方式(第八节)
Step 8 (15 min)  最终部署,核对一遍移动端体验
Step 9 (5 min)   发到班群
```

**总时间估算**:6-10 小时。考前 36 小时足够,但建议**Step 1-2 优先做完**,先有 URL 才能传播。

---

## 十三、致谢与版权

- **网站界面与功能设计**:Claude 4.7 Opus(Anthropic)
- **题库素材**:学校 e2e 教学平台(版权归学校及原作者所有)
- **本项目**:仅供同班学习,MIT License

---

## 附录 A:如何运行项目本地预览

```bash
# 进入项目目录
cd g2g/

# Python 自带的简单服务器(避免 file:// 的 CORS 问题)
python -m http.server 8000

# 浏览器打开 http://localhost:8000/
```

**注意**:直接双击 `index.html` 用 `file://` 协议打开,`fetch('data/questions.json')` 会被浏览器 CORS 策略拦截。必须用 HTTP 服务器(本地任意 server 都行)。

## 附录 B:常见问题

**Q:fetch 在微信内置浏览器里有兼容问题吗?**
A:`fetch` 在 iOS 11+ 和 Android Chrome 都支持,微信 X5 内核也支持。无需 polyfill。

**Q:`localStorage` 在隐私模式下会失败吗?**
A:Safari 隐私模式下 `localStorage` 配额为 0,写入会抛错。需要 try-catch 包裹所有 localStorage 调用。当前 HTML 已经在关键位置做了容错。

**Q:可以加 Google Analytics 吗?**
A:不建议。本项目主打"零追踪",这是 README 的承诺。

**Q:章节名字怎么改?**
A:改 `md_to_json.py` 顶部的 `CHAPTER_NAMES` 字典,重跑脚本。

**Q:发现 AI 答案有错,怎么修?**
A:直接编辑 `data/questions.json`,改对应题的 `answer` 和 `explanation` 字段,提交 PR。也可以把 `confidence` 改为 `"high"` 标识为已校对。
